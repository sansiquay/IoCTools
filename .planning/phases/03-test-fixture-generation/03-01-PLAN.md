---
phase: 03-test-fixture-generation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - IoCTools.sln
  - IoCTools.Testing.Abstractions/IoCTools.Testing.Abstractions.csproj
  - IoCTools.Testing.Abstractions/Annotations/CoverAttribute.cs
  - IoCTools.Testing/IoCTools.Testing/IoCTools.Testing.csproj
  - IoCTools.Testing/IoCTools.Testing/IoCTools.TestingGenerator.cs
autonomous: true
requirements: [TEST-01]
user_setup: []

must_haves:
  truths:
    - "CoverAttribute exists in IoCTools.Testing.Abstractions namespace"
    - "CoverAttribute is generic with single type parameter TService"
    - "CoverAttribute usage is restricted to Class targets only"
    - "IoCTools.Testing.Abstractions targets net8.0"
    - "IoCTools.Testing targets net8.0 with Moq 4.20.72 dependency"
    - "IoCTools.Testing is configured as analyzer package"
    - "Both projects added to solution"
  artifacts:
    - path: "IoCTools.Testing.Abstractions/IoCTools.Testing.Abstractions.csproj"
      provides: "Project file for testing abstractions"
      contains: "TargetFramework>net8.0<"
    - path: "IoCTools.Testing.Abstractions/Annotations/CoverAttribute.cs"
      provides: "Cover<T> attribute for test fixture generation"
      exports: ["CoverAttribute"]
    - path: "IoCTools.Testing/IoCTools.Testing/IoCTools.Testing.csproj"
      provides: "Source generator project configuration"
      contains: "Moq"
    - path: "IoCTools.Testing/IoCTools.Testing/IoCTools.TestingGenerator.cs"
      provides: "IIncrementalGenerator entry point"
      exports: ["IoCToolsTestingGenerator"]
  key_links:
    - from: "IoCTools.Testing/IoCTools.Testing.csproj"
      to: "IoCTools.Testing.Abstractions"
      via: "ProjectReference"
      pattern: "ProjectReference.*IoCTools.Testing.Abstractions"
    - from: "IoCTools.Testing/IoCTools.Testing.csproj"
      to: "Moq"
      via: "PackageReference"
      pattern: "PackageReference.*Moq.*4.20.72"
---

<objective>
Create IoCTools.Testing package structure with CoverAttribute and generator skeleton

Purpose: Establish the project foundation for test fixture generation — two new projects (IoCTools.Testing.Abstractions for the attribute, IoCTools.Testing for the generator) that will ship as a separate NuGet package with Moq dependency.
Output: Two new projects added to solution, CoverAttribute defined, generator stub initialized
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-test-fixture-generation/03-CONTEXT.md
@.planning/phases/03-test-fixture-generation/03-RESEARCH.md
@.planning/STATE.md
@.planning/ROADMAP.md

# Reference implementation patterns
@IoCTools.Abstractions/IoCTools.Abstractions.csproj
@IoCTools.Abstractions/Annotations/ScopedAttribute.cs
@IoCTools.Abstractions/Annotations/InjectAttribute.cs
@IoCTools.Generator/IoCTools.Generator/DependencyInjectionGenerator.cs
@IoCTools.Generator.Tests/IoCTools.Generator.Tests.csproj
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create IoCTools.Testing.Abstractions project with CoverAttribute</name>
  <files>
    IoCTools.Testing.Abstractions/IoCTools.Testing.Abstractions.csproj
    IoCTools.Testing.Abstractions/Annotations/CoverAttribute.cs
  </files>
  <read_first>
    - IoCTools.Abstractions/IoCTools.Abstractions.csproj (for .csproj pattern)
    - IoCTools.Abstractions/Annotations/ScopedAttribute.cs (for attribute pattern)
  </read_first>
  <action>
    Create directory `IoCTools.Testing.Abstractions/` and `IoCTools.Testing.Abstractions/Annotations/`:

    **File 1: IoCTools.Testing.Abstractions/IoCTools.Testing.Abstractions.csproj**
    ```xml
    <Project Sdk="Microsoft.NET.Sdk">
      <PropertyGroup>
        <TargetFramework>net8.0</TargetFramework>
        <IsPackable>true</IsPackable>
        <Nullable>enable</Nullable>
        <LangVersion>latest</LangVersion>
        <GeneratePackageOnBuild>true</GeneratePackageOnBuild>
        <Version>1.5.0</Version>
        <PackageVersion>1.5.0</PackageVersion>
        <AssemblyVersion>1.5.0.0</AssemblyVersion>
        <FileVersion>1.5.0.0</FileVersion>
        <Title>IoC Tools Testing Abstractions Package</Title>
        <Copyright>MIT</Copyright>
        <PackageProjectUrl>https://github.com/nate123456/IoCTools</PackageProjectUrl>
        <RepositoryUrl>https://github.com/nate123456/IoCTools</RepositoryUrl>
        <RepositoryType>git</RepositoryType>
        <PackageLicenseExpression>MIT</PackageLicenseExpression>
        <PackageReadmeFile>README.md</PackageReadmeFile>
        <Description>Test fixture generation attributes for IoCTools - mark test classes with [Cover&lt;TService&gt;] to auto-generate Mock&lt;T&gt; fields and CreateSut() factories.</Description>
      </PropertyGroup>
      <ItemGroup>
        <None Include="../../README.md" Pack="true" PackagePath="\" Visible="false"/>
      </ItemGroup>
    </Project>
    ```

    **File 2: IoCTools.Testing.Abstractions/Annotations/CoverAttribute.cs**
    ```csharp
    namespace IoCTools.Testing.Annotations;

    using System;

    /// <summary>
    /// Marks a test class to receive auto-generated test fixture members for the specified service.
    /// The test class must be partial. Generated members include:
    /// - Mock&lt;T&gt; fields for each constructor dependency
    /// - CreateSut() factory method wiring all mocks
    /// - Typed Setup{Dependency}(Action&lt;Mock&lt;T&gt;&gt;) helper methods
    /// </summary>
    /// <typeparam name="TService">The service class whose constructor signature defines the fixture dependencies</typeparam>
    [AttributeUsage(AttributeTargets.Class, AllowMultiple = false)]
    public sealed class CoverAttribute<TService> : Attribute
        where TService : class
    {
    }
    ```
  </action>
  <verify>
    <automated>dotnet build IoCTools.Testing.Abstractions/IoCTools.Testing.Abstractions.csproj --no-restore 2>&1 | grep -E "(error|warning|succeeded)"</automated>
  </verify>
  <done>
    - IoCTools.Testing.Abstractions/IoCTools.Testing.Abstractions.csproj exists and targets net8.0
    - CoverAttribute<TService> defined with class-only AttributeUsage and AllowMultiple=false
    - Project builds successfully with no errors
    - Version set to 1.5.0 for milestone packaging
  </done>
</task>

<task type="auto">
  <name>Task 2: Create IoCTools.Testing generator project with Moq dependency</name>
  <files>
    IoCTools.Testing/IoCTools.Testing/IoCTools.Testing.csproj
    IoCTools.Testing/IoCTools.Testing/IoCTools.TestingGenerator.cs
  </files>
  <read_first>
    - IoCTools.Generator/IoCTools.Generator/IoCTools.Generator.csproj (for analyzer project pattern)
    - IoCTools.Generator/IoCTools.Generator/DependencyInjectionGenerator.cs (for generator pattern)
  </read_first>
  <action>
    Create directory `IoCTools.Testing/IoCTools.Testing/`:

    **File 1: IoCTools.Testing/IoCTools.Testing/IoCTools.Testing.csproj**
    ```xml
    <Project Sdk="Microsoft.NET.Sdk">
      <PropertyGroup>
        <TargetFramework>net8.0</TargetFramework>
        <IsPackable>true</IsPackable>
        <Nullable>enable</Nullable>
        <LangVersion>latest</LangVersion>
        <GeneratePackageOnBuild>true</GeneratePackageOnBuild>
        <Version>1.5.0</Version>
        <PackageVersion>1.5.0</PackageVersion>
        <AssemblyVersion>1.5.0.0</AssemblyVersion>
        <FileVersion>1.5.0.0</FileVersion>
        <Title>IoC Tools Testing Source Generator</Title>
        <Copyright>MIT</Copyright>
        <PackageProjectUrl>https://github.com/nate123456/IoCTools</PackageProjectUrl>
        <RepositoryUrl>https://github.com/nate123456/IoCTools</RepositoryUrl>
        <RepositoryType>git</RepositoryType>
        <PackageLicenseExpression>MIT</PackageLicenseExpression>
        <PackageReadmeFile>README.md</PackageReadmeFile>
        <Description>Source generator for IoCTools test fixture generation - generates Mock&lt;T&gt; fields, CreateSut() factories, and typed setup helpers for test classes marked with [Cover&lt;TService&gt;].</Description>
        <IncludeBuildOutput>false</IncludeBuildOutput>
        <DevelopmentDependency>true</DevelopmentDependency>
        <CompilerVisibleProperties>IoCToolsTestingDiagnosticSeverity</CompilerVisibleProperties>
      </PropertyGroup>
      <ItemGroup>
        <PackageReference Include="Microsoft.CodeAnalysis.CSharp" Version="4.5.0"/>
        <PackageReference Include="Microsoft.CodeAnalysis.Analyzers" Version="3.3.4" PrivateAssets="all"/>
        <PackageReference Include="Moq" Version="4.20.72"/>
      </ItemGroup>
      <ItemGroup>
        <ProjectReference Include="..\..\IoCTools.Testing.Abstractions\IoCTools.Testing.Abstractions.csproj"/>
      </ItemGroup>
      <ItemGroup>
        <None Include="../../README.md" Pack="true" PackagePath="\" Visible="false"/>
      </ItemGroup>
      <ItemGroup>
        <!-- Package the generator in the analyzer directory of the nuget package -->
        <None Include="$(OutputPath)\$(AssemblyName).dll" Pack="true" PackagePath="analyzers\dotnet\cs" Visible="false"/>
        <None Include="$(OutputPath)\$(AssemblyName).dll" Pack="true" PackagePath="analyzers/dotnet/cs" Visible="false"/>
      </ItemGroup>
    </Project>
    ```

    **File 2: IoCTools.Testing/IoCTools.Testing/IoCTools.TestingGenerator.cs**
    ```csharp
    namespace IoCTools.Testing;

    using Microsoft.CodeAnalysis;

    [Generator]
    public sealed class IoCToolsTestingGenerator : IIncrementalGenerator
    {
        public void Initialize(IncrementalGeneratorInitializationContext context)
        {
            // Test fixture pipeline will be attached in 03-02
            // Analyzer diagnostics will be attached in 03-03
        }
    }
    ```
  </action>
  <verify>
    <automated>dotnet build IoCTools.Testing/IoCTools.Testing/IoCTools.Testing.csproj 2>&1 | grep -E "(error|warning|succeeded)"</automated>
  </verify>
  <done>
    - IoCTools.Testing/IoCTools.Testing/IoCTools.Testing.csproj exists
    - Project targets net8.0 with Moq 4.20.72 PackageReference
    - Project references IoCTools.Testing.Abstractions
    - IoCToolsTestingGenerator stub exists with IIncrementalGenerator
    - Analyzer packaging configured (IncludeBuildOutput=false, DevelopmentDependency=true)
    - CompilerVisibleProperty for IoCToolsTestingDiagnosticSeverity configured
    - Project builds successfully with no errors
  </done>
</task>

<task type="auto">
  <name>Task 3: Add both projects to solution and verify build</name>
  <files>
    IoCTools.sln
  </files>
  <read_first>
    - IoCTools.sln (to understand existing project entry format)
  </read_first>
  <action>
    Add the two new projects to IoCTools.sln. Insert these Project entries after the IoCTools.Abstractions entry (line 5, before IoCTools.Sample):

    **For IoCTools.Testing.Abstractions:**
    ```
    Project("{FAE04EC0-301F-11D3-BF4B-00C04F79EFBC}") = "IoCTools.Testing.Abstractions", "IoCTools.Testing.Abstractions\IoCTools.Testing.Abstractions.csproj", "{GENERATE_NEW_GUID_1}"
    EndProject
    ```

    **For IoCTools.Testing:**
    ```
    Project("{FAE04EC0-301F-11D3-BF4B-00C04F79EFBC}") = "IoCTools.Testing", "IoCTools.Testing\IoCTools.Testing\IoCTools.Testing.csproj", "{GENERATE_NEW_GUID_2}"
    EndProject
    ```

    Add corresponding GlobalSection(ProjectConfigurationPlatforms) entries for both projects following the pattern of existing projects (Debug|Any CPU, Release|Any CPU configurations).

    Then verify the full solution builds:
    ```bash
    dotnet build IoCTools.sln --no-incremental
    ```
  </action>
  <verify>
    <automated>dotnet build IoCTools.sln --no-incremental 2>&1 | tail -5</automated>
  </verify>
  <done>
    - IoCTools.sln contains both new projects with unique GUIDs
    - GlobalSection(ProjectConfigurationPlatforms) includes all Debug/Release configurations
    - Solution builds successfully (6 projects total)
    - No project reference errors
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Verify `dotnet build IoCTools.sln` succeeds for all 6 projects
2. Verify CoverAttribute is public and has [AttributeUsage(AttributeTargets.Class, AllowMultiple = false)]
3. Verify IoCTools.Testing.csproj has Moq 4.20.72 PackageReference
4. Verify both projects have Version 1.5.0 and GeneratePackageOnBuild=true
5. Verify IoCTools.Testing has IncludeBuildOutput=false and DevelopmentDependency=true
</verification>

<success_criteria>
1. Two new projects (IoCTools.Testing.Abstractions, IoCTools.Testing) exist and build
2. CoverAttribute<TService> is defined with correct constraints and usage
3. IoCTools.Testing references Moq 4.20.72 as direct dependency
4. Solution includes both projects and builds successfully
5. Generator stub is ready for pipeline implementation in 03-02
</success_criteria>

<output>
After completion, create `.planning/phases/03-test-fixture-generation/03-01-SUMMARY.md`
</output>
