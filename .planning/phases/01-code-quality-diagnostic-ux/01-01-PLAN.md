---
phase: 01-code-quality-diagnostic-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - IoCTools.Generator/IoCTools.Generator/Diagnostics/Descriptors/LifetimeDiagnostics.cs
  - IoCTools.Generator/IoCTools.Generator/Diagnostics/Descriptors/StructuralDiagnostics.cs
  - IoCTools.Generator/IoCTools.Generator/Diagnostics/Descriptors/RegistrationDiagnostics.cs
  - IoCTools.Generator/IoCTools.Generator/Diagnostics/Descriptors/DependencyDiagnostics.cs
  - IoCTools.Generator/IoCTools.Generator/Diagnostics/Descriptors/ConfigurationDiagnostics.cs
  - IoCTools.Generator/IoCTools.Generator/Generator/Diagnostics/Validators/LifetimeDependencyValidator.cs
  - IoCTools.Generator/IoCTools.Generator/Generator/Diagnostics/Validators/ConfigurationInjectionValidator.cs
  - docs/diagnostics.md
autonomous: true
requirements:
  - DUX-01
  - DUX-02
  - DUX-03
  - DUX-04
  - DUX-05

must_haves:
  truths:
    - "Every diagnostic descriptor has a non-empty HelpLinkUri"
    - "Every diagnostic descriptor uses a specific IoCTools.{Subcategory} category instead of generic 'IoCTools'"
    - "IOC012/013 messages suggest IServiceProvider/CreateScope() as a fix option"
    - "IOC015 message includes the full inheritance path"
    - "IOC016-019 descriptions include configuration usage examples"
    - "docs/diagnostics.md has an anchor for every diagnostic"
  artifacts:
    - path: "docs/diagnostics.md"
      provides: "HelpLinkUri target with anchors for all 87 diagnostics"
      contains: "## IOC001"
    - path: "IoCTools.Generator/IoCTools.Generator/Diagnostics/Descriptors/LifetimeDiagnostics.cs"
      provides: "10 descriptors with helpLinkUri and IoCTools.Lifetime category"
      contains: "IoCTools.Lifetime"
    - path: "IoCTools.Generator/IoCTools.Generator/Diagnostics/Descriptors/DependencyDiagnostics.cs"
      provides: "27 descriptors with helpLinkUri and IoCTools.Dependency category"
      contains: "IoCTools.Dependency"
    - path: "IoCTools.Generator/IoCTools.Generator/Diagnostics/Descriptors/ConfigurationDiagnostics.cs"
      provides: "10 descriptors with helpLinkUri and IoCTools.Configuration category"
      contains: "IoCTools.Configuration"
    - path: "IoCTools.Generator/IoCTools.Generator/Diagnostics/Descriptors/RegistrationDiagnostics.cs"
      provides: "27 descriptors with helpLinkUri and IoCTools.Registration category"
      contains: "IoCTools.Registration"
    - path: "IoCTools.Generator/IoCTools.Generator/Diagnostics/Descriptors/StructuralDiagnostics.cs"
      provides: "13 descriptors with helpLinkUri and IoCTools.Structural category"
      contains: "IoCTools.Structural"
  key_links:
    - from: "docs/diagnostics.md"
      to: "DiagnosticDescriptor helpLinkUri values"
      via: "URL anchors matching diagnostic IDs"
      pattern: "diagnostics\\.md#ioc0"
    - from: "LifetimeDependencyValidator.cs"
      to: "IOC015 descriptor"
      via: "Diagnostic.Create with path format arg"
      pattern: "InheritanceChainLifetimeValidation"
---

<objective>
Add HelpLinkUri, specific IDE categories, and enhanced diagnostic messages to all 87 diagnostic descriptors, and create the docs/diagnostics.md reference file.

Purpose: Polish diagnostic UX so every diagnostic is clickable in IDEs, filterable by category, and actionable with clear fix guidance including IServiceProvider/CreateScope() suggestions and inheritance path display.
Output: Updated 5 descriptor files, updated 2 validator files, new docs/diagnostics.md
</objective>

<execution_context>
@/Users/nathan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nathan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-code-quality-diagnostic-ux/01-CONTEXT.md
@.planning/phases/01-code-quality-diagnostic-ux/01-RESEARCH.md

<interfaces>
<!-- Diagnostic descriptor files all follow this 7-arg constructor pattern. Migrate to 8-arg by adding helpLinkUri. -->

Current constructor pattern (all 87 descriptors):
```csharp
public static readonly DiagnosticDescriptor Name = new(
    "IOC0XX",           // id
    "Title",            // title
    "Message {0}",      // messageFormat
    "IoCTools",         // category -- CHANGE to IoCTools.{Subcategory}
    DiagnosticSeverity, // defaultSeverity
    true,               // isEnabledByDefault
    "Description.");    // description -- ADD helpLinkUri after this
```

Target 8-arg pattern:
```csharp
public static readonly DiagnosticDescriptor Name = new(
    "IOC0XX",
    "Title",
    "Message {0}",
    "IoCTools.Lifetime",  // specific category
    DiagnosticSeverity,
    true,
    "Description.",
    "https://github.com/nathan-p-lane/IoCTools/blob/main/docs/diagnostics.md#ioc0xx");
```

From LifetimeDependencyValidator.cs (IOC015 emit site, lines 37-38):
```csharp
var diagnostic = Diagnostic.Create(descriptor,
    classDeclaration.GetLocation(), classSymbol.Name, serviceLifetime, baseServiceLifetime);
// Format args: {0}=classSymbol.Name, {1}=serviceLifetime, {2}=baseServiceLifetime
// Need to add {3} for full inheritance path
```

Category-to-file mapping (per D-05):
- LifetimeDiagnostics.cs (10) -> "IoCTools.Lifetime"
- DependencyDiagnostics.cs (27) -> "IoCTools.Dependency"
- ConfigurationDiagnostics.cs (10) -> "IoCTools.Configuration"
- RegistrationDiagnostics.cs (27) -> "IoCTools.Registration"
- StructuralDiagnostics.cs (13) -> "IoCTools.Structural"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add HelpLinkUri and IDE categories to all 87 diagnostic descriptors</name>
  <files>
    IoCTools.Generator/IoCTools.Generator/Diagnostics/Descriptors/LifetimeDiagnostics.cs,
    IoCTools.Generator/IoCTools.Generator/Diagnostics/Descriptors/StructuralDiagnostics.cs,
    IoCTools.Generator/IoCTools.Generator/Diagnostics/Descriptors/RegistrationDiagnostics.cs,
    IoCTools.Generator/IoCTools.Generator/Diagnostics/Descriptors/DependencyDiagnostics.cs,
    IoCTools.Generator/IoCTools.Generator/Diagnostics/Descriptors/ConfigurationDiagnostics.cs
  </files>
  <read_first>
    IoCTools.Generator/IoCTools.Generator/Diagnostics/Descriptors/LifetimeDiagnostics.cs,
    IoCTools.Generator/IoCTools.Generator/Diagnostics/Descriptors/StructuralDiagnostics.cs,
    IoCTools.Generator/IoCTools.Generator/Diagnostics/Descriptors/RegistrationDiagnostics.cs,
    IoCTools.Generator/IoCTools.Generator/Diagnostics/Descriptors/DependencyDiagnostics.cs,
    IoCTools.Generator/IoCTools.Generator/Diagnostics/Descriptors/ConfigurationDiagnostics.cs
  </read_first>
  <action>
For each of the 87 diagnostic descriptors across all 5 files, make two changes:

1. **Change category** (4th constructor arg) from `"IoCTools"` to the file-specific category:
   - LifetimeDiagnostics.cs: `"IoCTools.Lifetime"` (IOC012, IOC013, IOC014, IOC015, IOC033, IOC059, IOC060, IOC075, IOC084, IOC087)
   - StructuralDiagnostics.cs: `"IoCTools.Structural"` (IOC010, IOC011, IOC020-026, IOC058, IOC067, IOC068, IOC080)
   - RegistrationDiagnostics.cs: `"IoCTools.Registration"` (IOC004, IOC005, IOC027-032, IOC034-038, IOC047, IOC063-065, IOC069-072, IOC074, IOC081-083, IOC085-086)
   - DependencyDiagnostics.cs: `"IoCTools.Dependency"` (IOC001-003, IOC006-009, IOC039-045, IOC048-055, IOC061-062, IOC076-078)
   - ConfigurationDiagnostics.cs: `"IoCTools.Configuration"` (IOC016-019, IOC046, IOC056-057, IOC079, IOC088-089)

2. **Add helpLinkUri** as 8th positional constructor argument after the description string:
   - URL pattern: `"https://github.com/nathan-p-lane/IoCTools/blob/main/docs/diagnostics.md#iocXXX"` where XXX is the zero-padded 3-digit diagnostic number
   - Example for IOC012: `"https://github.com/nathan-p-lane/IoCTools/blob/main/docs/diagnostics.md#ioc012"`
   - Example for IOC001: `"https://github.com/nathan-p-lane/IoCTools/blob/main/docs/diagnostics.md#ioc001"`

3. **Enhance IOC012 description** (in LifetimeDiagnostics.cs):
   Change the description from:
   `"Fix the lifetime mismatch by: 1) Changing dependency '{1}' to [Singleton], 2) Changing this service to [Scoped] or [Transient], or 3) Use dependency factories/scoped service locator pattern."`
   To:
   `"Fix the lifetime mismatch by: 1) Changing dependency '{1}' to [Singleton], 2) Changing this service to [Scoped] or [Transient], 3) Inject IServiceProvider and call CreateScope() to resolve '{1}' on demand, or 4) Use a factory delegate Func<{1}> to create instances per-use."`

4. **Enhance IOC013 description** (in LifetimeDiagnostics.cs):
   Change the description from:
   `"Review the design: 1) If '{1}' should be shared, change it to [Singleton], 2) If truly transient, this may cause issues as the singleton will capture only one instance - consider using IServiceProvider or factory pattern instead."`
   To:
   `"Review the design: 1) If '{1}' should be shared, change it to [Singleton], 2) If truly transient, inject IServiceProvider and call CreateScope() to resolve '{1}' on demand, or 3) Use a factory delegate Func<{1}> to create a new instance each time it is needed."`

5. **Enhance IOC087 description** (in LifetimeDiagnostics.cs):
   Change the description from:
   `"Fix the lifetime mismatch by: 1) Changing dependency '{1}' to [Singleton] or [Transient], 2) Changing this service to [Scoped], or 3) Use dependency factories/scoped service locator pattern."`
   To:
   `"Fix the lifetime mismatch by: 1) Changing dependency '{1}' to [Singleton] or [Transient], 2) Changing this service to [Scoped], 3) Inject IServiceProvider and call CreateScope() to resolve '{1}' on demand, or 4) Use a factory delegate Func<{1}> to create instances per-use."`

6. **Enhance IOC015 message format** (in LifetimeDiagnostics.cs):
   Change the messageFormat from:
   `"Service lifetime mismatch in inheritance chain: '{0}' ({1}) inherits from dependencies with {2} lifetime"`
   To:
   `"Service lifetime mismatch in inheritance chain: '{0}' ({1}) inherits from dependencies with {2} lifetime. Inheritance path: {3}"`

7. **Enhance IOC016 description** (in ConfigurationDiagnostics.cs):
   Change from: `"Provide a valid configuration key. Keys cannot be empty, whitespace-only, or contain invalid characters like double colons."`
   To: `"Provide a valid configuration key. Keys cannot be empty, whitespace-only, or contain invalid characters like double colons. Example valid keys: 'ConnectionStrings:Default', 'App:Settings:Feature'."`

8. **Enhance IOC017 description** (in ConfigurationDiagnostics.cs):
   Change from: `"Use a supported configuration type (primitives, POCOs with parameterless constructors, collections) or provide a custom converter."`
   To: `"Use a supported configuration type: primitives (string, int, bool, double), POCOs with parameterless constructors, or collections (List<T>, Dictionary<string, T>). Example: [InjectConfiguration(\"MySection\")] private readonly MyOptions _options; where MyOptions has a parameterless constructor."`

9. **Enhance IOC018 description** (in ConfigurationDiagnostics.cs):
   Append to existing: Add ` Example: 'public partial class {0} {{ [InjectConfiguration(\"Section\")] private readonly MyOptions _options; }}'` to the end.

10. **Enhance IOC019 description** (in ConfigurationDiagnostics.cs):
    Append to existing: Add ` Example: Change 'static readonly MyOptions _options' to 'private readonly MyOptions _options' and add [InjectConfiguration].` to the end.

CRITICAL: The description (7th arg) stays in position 7. The helpLinkUri goes in position 8. Do NOT swap them -- they are both string? so the compiler will not catch this error.
  </action>
  <verify>
    <automated>cd /Users/nathan/Documents/projects/IoCTools && dotnet build IoCTools.Generator/IoCTools.Generator/IoCTools.Generator.csproj --no-restore 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -c '"IoCTools.Lifetime"' IoCTools.Generator/IoCTools.Generator/Diagnostics/Descriptors/LifetimeDiagnostics.cs returns 10
    - grep -c '"IoCTools.Structural"' IoCTools.Generator/IoCTools.Generator/Diagnostics/Descriptors/StructuralDiagnostics.cs returns 13
    - grep -c '"IoCTools.Registration"' IoCTools.Generator/IoCTools.Generator/Diagnostics/Descriptors/RegistrationDiagnostics.cs returns 27
    - grep -c '"IoCTools.Dependency"' IoCTools.Generator/IoCTools.Generator/Diagnostics/Descriptors/DependencyDiagnostics.cs returns 27
    - grep -c '"IoCTools.Configuration"' IoCTools.Generator/IoCTools.Generator/Diagnostics/Descriptors/ConfigurationDiagnostics.cs returns 10
    - grep -c 'diagnostics.md#ioc' across all 5 descriptor files totals 87
    - grep '"IoCTools",' across all 5 descriptor files returns 0 matches (no remaining generic category)
    - LifetimeDiagnostics.cs contains "CreateScope()" in IOC012 and IOC013 descriptions
    - LifetimeDiagnostics.cs IOC015 messageFormat contains "Inheritance path: {3}"
    - ConfigurationDiagnostics.cs IOC016 description contains "ConnectionStrings:Default"
    - ConfigurationDiagnostics.cs IOC017 description contains "parameterless constructor"
    - dotnet build IoCTools.Generator succeeds with 0 errors
  </acceptance_criteria>
  <done>All 87 diagnostic descriptors have IoCTools.{Subcategory} categories, helpLinkUri values, and IOC012/013/015/016-019 have enhanced messages</done>
</task>

<task type="auto">
  <name>Task 2: Update IOC015 emit sites for inheritance path and create docs/diagnostics.md</name>
  <files>
    IoCTools.Generator/IoCTools.Generator/Generator/Diagnostics/Validators/LifetimeDependencyValidator.cs,
    docs/diagnostics.md
  </files>
  <read_first>
    IoCTools.Generator/IoCTools.Generator/Generator/Diagnostics/Validators/LifetimeDependencyValidator.cs,
    IoCTools.Generator/IoCTools.Generator/Diagnostics/Descriptors/LifetimeDiagnostics.cs,
    IoCTools.Generator/IoCTools.Generator/Diagnostics/Descriptors/StructuralDiagnostics.cs,
    IoCTools.Generator/IoCTools.Generator/Diagnostics/Descriptors/RegistrationDiagnostics.cs,
    IoCTools.Generator/IoCTools.Generator/Diagnostics/Descriptors/DependencyDiagnostics.cs,
    IoCTools.Generator/IoCTools.Generator/Diagnostics/Descriptors/ConfigurationDiagnostics.cs
  </read_first>
  <action>
**Part A: Update IOC015 Diagnostic.Create() calls in LifetimeDependencyValidator.cs**

The IOC015 message format now has 4 placeholders: `{0}`=className, `{1}`=serviceLifetime, `{2}`=conflictingLifetime, `{3}`=inheritancePath.

In `ValidateInheritanceChainLifetimesForSourceProduction()`, there are two `Diagnostic.Create()` calls using `InheritanceChainLifetimeValidation`. Both need a 4th format arg for the inheritance path.

**Site 1 (line ~37-38):** The base type lifetime conflict. Build the path by collecting type names from classSymbol up through the chain to currentType:
```csharp
// Before the Diagnostic.Create call, build inheritance path string
var pathParts = new List<string>();
var pathType = classSymbol;
while (pathType != null && pathType.SpecialType != SpecialType.System_Object)
{
    pathParts.Add(pathType.Name);
    if (SymbolEqualityComparer.Default.Equals(pathType, currentType)) break;
    pathType = pathType.BaseType;
}
var inheritancePath = string.Join(" -> ", pathParts);

var diagnostic = Diagnostic.Create(descriptor,
    classDeclaration.GetLocation(), classSymbol.Name, serviceLifetime, baseServiceLifetime, inheritancePath);
```

**Site 2 (line ~59-62):** The DependsOn attribute lifetime conflict within the inheritance chain. Build the path similarly, but append the dependency type name at the end:
```csharp
var pathParts2 = new List<string>();
var pathType2 = classSymbol;
while (pathType2 != null && pathType2.SpecialType != SpecialType.System_Object)
{
    pathParts2.Add(pathType2.Name);
    if (SymbolEqualityComparer.Default.Equals(pathType2, currentType)) break;
    pathType2 = pathType2.BaseType;
}
pathParts2.Add(typeArg.Name);
var inheritancePath2 = string.Join(" -> ", pathParts2);

var diagnostic = Diagnostic.Create(descriptor,
    classDeclaration.GetLocation(), classSymbol.Name, serviceLifetime, depLifetime, inheritancePath2);
```

Add `using System.Collections.Generic;` if not already present (needed for `List<string>`). Also add `using IoCTools.Generator.Utilities;` if not present (needed for `SymbolEqualityComparer` usage -- though it may already be via a global using or existing import).

**Part B: Create docs/diagnostics.md**

Create `/Users/nathan/Documents/projects/IoCTools/docs/diagnostics.md` with entries for all 87 diagnostics. Use this format per entry (per D-03: lean content -- code, severity, one-line cause, one-line fix):

```markdown
# IoCTools Diagnostics Reference

Quick reference for all IoCTools diagnostic messages. For detailed examples and guidance, see the full documentation.

## IOC001

**Severity:** Error
**Category:** IoCTools.Dependency

**Cause:** No implementation of the depended-upon interface exists in the project.

**Fix:** Create a class implementing the interface with a lifetime attribute, add `[ExternalService]`, or register manually.

---
```

Generate all 87 entries. Pull the diagnostic ID, severity, and title from the descriptor files read in read_first. Each anchor must be lowercase (e.g., `#ioc001`, `#ioc012`) to match the helpLinkUri pattern `docs/diagnostics.md#iocXXX`.

The file must contain these anchors (verified by the helpLinkUri URLs):
- IOC001-003, IOC004-009, IOC010-015, IOC016-019, IOC020-027, IOC028-032, IOC033-040, IOC041-048, IOC049-055, IOC056-060, IOC061-065, IOC067-072, IOC074-080, IOC081-089

Note: There are gaps in the numbering (no IOC066, IOC073). Only create entries for diagnostics that have actual descriptors in the codebase.
  </action>
  <verify>
    <automated>cd /Users/nathan/Documents/projects/IoCTools && dotnet build --no-restore 2>&amp;1 | grep -c "error " && test -f docs/diagnostics.md && grep -c "^## IOC" docs/diagnostics.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/diagnostics.md exists and contains exactly 87 `## IOC` headings
    - docs/diagnostics.md contains `## IOC001`, `## IOC012`, `## IOC015`, `## IOC086`, `## IOC089`
    - docs/diagnostics.md does NOT contain `## IOC066` or `## IOC073` (these don't exist in codebase)
    - Every `## IOC` heading in docs/diagnostics.md has a corresponding **Severity:** line
    - Every `## IOC` heading in docs/diagnostics.md has a corresponding **Category:** line containing one of: IoCTools.Lifetime, IoCTools.Dependency, IoCTools.Configuration, IoCTools.Registration, IoCTools.Structural
    - LifetimeDependencyValidator.cs IOC015 Diagnostic.Create calls now pass 4 format args (not 3)
    - LifetimeDependencyValidator.cs contains `string.Join(" -> "` for building inheritance path
    - dotnet build succeeds with 0 errors (full solution build)
    - cd IoCTools.Generator.Tests && dotnet test --no-build passes
  </acceptance_criteria>
  <done>IOC015 emit sites pass full inheritance path as {3} format arg, and docs/diagnostics.md has anchored entries for all 87 diagnostics matching the helpLinkUri pattern</done>
</task>

</tasks>

<verification>
1. `dotnet build` succeeds for the full solution
2. `dotnet test --project IoCTools.Generator.Tests` passes (all 1650+ tests)
3. All 87 descriptors have non-empty helpLinkUri (grep confirms 87 matches for `diagnostics.md#ioc`)
4. No descriptor uses the generic `"IoCTools"` category (grep confirms 0 matches for `"IoCTools",` across descriptor files)
5. IOC012/013 descriptions contain "CreateScope()"
6. IOC015 message format contains `{3}` for inheritance path
7. docs/diagnostics.md has 87 diagnostic entries
</verification>

<success_criteria>
- All 87 diagnostic descriptors updated with IoCTools.{Subcategory} category and helpLinkUri
- IOC012/013/087 descriptions include IServiceProvider/CreateScope() suggestion
- IOC015 message format includes inheritance path display
- IOC016-019 descriptions include configuration examples
- docs/diagnostics.md exists with 87 anchored entries
- Full solution builds with zero errors
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-code-quality-diagnostic-ux/01-01-SUMMARY.md`
</output>
