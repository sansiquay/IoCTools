---
phase: 04-documentation
plan: 04
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
  - 04-03
files_modified:
  - docs/diagnostics.md
  - CONTRIBUTING.md
autonomous: true
requirements:
  - DOC-05
  - DOC-09
must_haves:
  truths:
    - docs/diagnostics.md is enhanced with category navigation sections
    - docs/diagnostics.md includes typeof() diagnostics (IOC090-IOC094)
    - docs/diagnostics.md includes test fixture diagnostics (TDIAG-01 through TDIAG-05)
    - docs/diagnostics.md has severity badges and cross-references
    - CONTRIBUTING.md references platform constraints documentation
    - All HelpLinkUri anchors resolve to existing sections in docs/diagnostics.md
  artifacts:
    - path: "docs/diagnostics.md"
      provides: "Complete diagnostic reference with enhancements"
      contains: "## Dependency Diagnostics", "## Lifetime Diagnostics", "## Configuration Diagnostics", "## Registration Diagnostics", "## Structural Diagnostics", "## Testing Diagnostics"
    - path: "CONTRIBUTING.md"
      provides: "Updated contribution guidelines with platform constraints link"
      contains: "Platform constraints", "docs/platform-constraints.md"
  key_links:
    - from: "README.md"
      to: "docs/diagnostics.md"
      via: "Error-only diagnostic table"
      pattern: "\\[.*\\]\\(docs/diagnostics\\.md\\)"
    - from: "docs/diagnostics.md"
      to: "README.md"
      via: "back-link"
      pattern: "\\[.*\\]\\(../README\\.md\\)"
    - from: "CONTRIBUTING.md"
      to: "docs/platform-constraints.md"
      via: "reference"
      pattern: "\\[.*\\]\\(docs/platform-constraints\\.md\\)"
---

<objective>
Enhance docs/diagnostics.md with category navigation, severity badges, cross-references, and ensure all HelpLinkUri anchors resolve. Update CONTRIBUTING.md with platform constraints reference.

Purpose: Complete the diagnostic reference with enhancements from CONTEXT.md D-16 through D-19, add typeof() diagnostics (IOC090-IOC094) and test fixture diagnostics (TDIAG-01 through TDIAG-05), and verify all anchors resolve correctly.

Output: Enhanced docs/diagnostics.md + updated CONTRIBUTING.md
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@README.md
@docs/diagnostics.md
@.planning/phases/04-documentation/04-CONTEXT.md
@.planning/phases/04-documentation/04-RESEARCH.md
@.planning/phases/01-code-quality-diagnostic-ux/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance docs/diagnostics.md with category navigation, severity badges, and new diagnostics</name>
  <files>docs/diagnostics.md</files>
  <read_first>
    - docs/diagnostics.md (existing diagnostic reference - full file)
    - .planning/phases/04-documentation/04-CONTEXT.md (D-16 through D-19 for enhancement pattern)
    - .planning/phases/04-documentation/04-RESEARCH.md (Diagnostic Reference Enhancement Pattern)
  </read_first>
  <action>
Enhance docs/diagnostics.md following this structure:

1. **Add category navigation sections at the top:**
```markdown
# IoCTools Diagnostics Reference

Quick reference for all IoCTools diagnostic messages with remediation guidance.

## Diagnostic Categories

- [Dependency Diagnostics](#dependency-diagnostics) — IOC001-IOC002, IOC006-IOC009, IOC039-IOC055, IOC079
- [Lifetime Diagnostics](#lifetime-diagnostics) — IOC012-IOC015, IOC033, IOC059-IOC060, IOC072, IOC075, IOC084-IOC087
- [Configuration Diagnostics](#configuration-diagnostics) — IOC016-IOC019, IOC043, IOC046-IOC049, IOC056-IOC057, IOC088-IOC089
- [Registration Diagnostics](#registration-diagnostics) — IOC004-IOC005, IOC027-IOC038, IOC063-IOC065, IOC069-IOC074, IOC081-IOC086, IOC090-IOC094
- [Structural Diagnostics](#structural-diagnostics) — IOC010-IOC011, IOC020-IOC026, IOC041-IOC042, IOC068, IOC077-IOC078, IOC080
- [Testing Diagnostics](#testing-diagnostics) — TDIAG-01 through TDIAG-05

## Severity Legend

- [!Error](#) — Blocks compilation; must be fixed
- [!Warning](#) — Potential issue; should be reviewed
- [!Info](#) — Suggestion; can improve code quality

---

## Dependency Diagnostics (IOC001-IOC002, IOC006-IOC009, IOC039-IOC055, IOC079)

Diagnostics related to service dependencies, dependency sets, and dependency hygiene.
```

2. **Add severity badges to each diagnostic:**
Change existing format from:
```markdown
## IOC001

**Severity:** Error
```

To:
```markdown
## IOC001

**Severity:** [!Error](#) | **Category:** IoCTools.Dependency
```

3. **Add "Related" cross-references where applicable:**
```markdown
## IOC001

**Severity:** [!Error](#) | **Category:** IoCTools.Dependency

**Cause:** No implementation of the depended-upon interface exists in the project.

**Fix:** Create a class implementing the interface with a lifetime attribute, add `[ExternalService]`, or register manually.

**Related:** [IOC002](#ioc002) (implementation exists but not registered), [IOC042](#ioc042) (implementation available but marked external)

---
```

4. **Add new diagnostics sections for typeof() and test fixture diagnostics:**

Add typeof() diagnostics (IOC090-IOC094) after IOC089:

```markdown
## IOC090

**Severity:** [!Warning](#) | **Category:** IoCTools.Registration

**Cause:** A service is registered via `typeof()` but the implementation lacks IoCTools attributes.

**Fix:** Add `[Scoped]`, `[Singleton]`, or `[Transient]` (and `[RegisterAs]` if needed) instead of using `typeof()`.

**Example:**
```csharp
// Before (manual typeof registration):
services.AddScoped(typeof(IUserService), typeof(UserService));

// After (with IoCTools attributes):
[Scoped]
[RegisterAs<IUserService>]
public partial class UserService : IUserService { }
```

**Related:** [IOC091](#ioc091) (duplicate registration), [IOC092](#ioc092) (lifetime mismatch)

---

## IOC091

**Severity:** [!Warning](#) | **Category:** IoCTools.Registration

**Cause:** A service is registered via `typeof()` with the same lifetime that IoCTools already generates.

**Fix:** Remove the `typeof()` registration and rely on IoCTools attributes.

**Example:**
```csharp
// Manual registration duplicates IoCTools:
services.AddScoped(typeof(IUserService), typeof(UserService)); // IOC091

// Remove manual registration — IoCTools handles it
```

**Related:** [IOC081](#ioc081) (manual registration duplicates IoCTools)

---

## IOC092

**Severity:** [!Error](#) | **Category:** IoCTools.Registration

**Cause:** A service is registered via `typeof()` with a different lifetime than what IoCTools generates.

**Fix:** Align lifetimes or remove the `typeof()` registration.

**Example:**
```csharp
// IoCTools registers as Scoped:
[Scoped]
public partial class UserService : IUserService { }

// Manual Singleton registration causes mismatch:
services.AddSingleton(typeof(IUserService), typeof(UserService)); // IOC092

// Fix: Remove manual registration or change lifetime attribute
```

**Related:** [IOC082](#ioc082) (manual registration lifetime differs), [IOC012](#ioc012) (lifetime validation)

---

## IOC094

**Severity:** [!Info](#) | **Category:** IoCTools.Registration

**Cause:** An open generic is registered via `typeof()` (e.g., `typeof(IRepository<>)`).

**Fix:** This is informational only. IoCTools does not yet support open generic registration. Consider using closed generic registrations with IoCTools attributes.

**Example:**
```csharp
// Informational — open generics not yet supported:
services.AddScoped(typeof(IRepository<>), typeof(Repository<>)); // IOC094

// Workaround: Use closed generics with IoCTools attributes:
[Scoped]
[RegisterAs<IRepository<User>>]
public partial class UserRepository : IRepository<User> { }
```

---

## Testing Diagnostics (TDIAG-01 through TDIAG-05)

Diagnostics from the IoCTools.Testing package for test fixture generation.
```

5. **Ensure TDIAG diagnostics have full examples** (already present in existing docs/diagnostics.md - verify they remain)

6. **Add back-link to main README at the end:**
```markdown
---

**Back to [main README](../README.md)**
```
  </action>
  <verify>
    <automated>grep -q "## Diagnostic Categories" docs/diagnostics.md && grep -q "## Dependency Diagnostics" docs/diagnostics.md && grep -q "## Lifetime Diagnostics" docs/diagnostics.md && grep -q "## Configuration Diagnostics" docs/diagnostics.md && grep -q "## Registration Diagnostics" docs/diagnostics.md && grep -q "## Structural Diagnostics" docs/diagnostics.md && grep -q "## Testing Diagnostics" docs/diagnostics.md && grep -q "\\[!Error\\]" docs/diagnostics.md && grep -q "## IOC090" docs/diagnostics.md && grep -q "## IOC091" docs/diagnostics.md && grep -q "## IOC092" docs/diagnostics.md && grep -q "## IOC094" docs/diagnostics.md</automated>
  </verify>
  <done>
    docs/diagnostics.md enhanced with:
    - Diagnostic Categories navigation section at top
    - Severity Legend with badge examples
    - Category navigation sections (Dependency, Lifetime, Configuration, Registration, Structural, Testing)
    - Severity badges ([!Error], [!Warning], [!Info]) on all diagnostics
    - "Related" cross-references between related diagnostics
    - typeof() diagnostics (IOC090-IOC094) with examples
    - Testing Diagnostics section (TDIAG-01 through TDIAG-05)
    - Back-link to main README
  </done>
</task>

<task type="auto">
  <name>Task 2: Update CONTRIBUTING.md with platform constraints reference</name>
  <files>CONTRIBUTING.md</files>
  <read_first>
    - CONTRIBUTING.md (existing contribution guidelines)
    - .planning/phases/04-documentation/04-CONTEXT.md (D-26: multi-location strategy with CONTRIBUTING.md reference)
  </read_first>
  <action>
Update CONTRIBUTING.md to add platform constraints reference:

1. **Add new "Platform Constraints" section after "Code Style":**

```markdown
## Platform Constraints

IoCTools targets `netstandard2.0` for the generator internally, but your service code can use any .NET version and C# features. This distinction is important for contributors:

- **Generator code** (`IoCTools.Generator`, `IoCTools.Abstractions`) is limited to netstandard2.0 APIs
- **User service code** has no constraints from IoCTools

When contributing to the generator, avoid:
- Record types (`record struct`) — use classes/structs with manual equality
- `HashCode.Combine()` — use manual hash code implementation
- `Span<T>`/`Memory<T>` — limited availability in netstandard2.0

See [Platform Constraints Documentation](docs/platform-constraints.md) for full details on limitations and workarounds.
```

2. **Update Diagnostic Guidelines section to reference docs/diagnostics.md:**

Change existing:
```
## Diagnostic Guidelines

When adding new diagnostics:

1. Add descriptor to `DiagnosticDescriptors.cs`
2. Implement validator in `IoCTools.Generator/Generator/Diagnostics/Validators/`
3. Add tests in `IoCTools.Generator.Tests/`
4. Update `README.md` diagnostic reference table
5. Document severity configurability if applicable
```

To:
```markdown
## Diagnostic Guidelines

When adding new diagnostics:

1. Add descriptor to `DiagnosticDescriptors.cs` with HelpLinkUri pointing to `docs/diagnostics.md#iocXXX`
2. Implement validator in `IoCTools.Generator/Generator/Diagnostics/Validators/`
3. Add tests in `IoCTools.Generator.Tests/`
4. Update `docs/diagnostics.md` with the new diagnostic entry (including category, severity, cause, fix, examples)
5. Update README.md Error-only diagnostic table if severity is Error
6. Document severity configurability via MSBuild properties if applicable

**HelpLinkUri format:** `https://github.com/nathan-p-lane/IoCTools/blob/main/docs/diagnostics.md#iocXXX`

Ensure the anchor `#iocXXX` exists in docs/diagnostics.md before committing.
```
  </action>
  <verify>
    <automated>grep -q "## Platform Constraints" CONTRIBUTING.md && grep -q "docs/platform-constraints.md" CONTRIBUTING.md && grep -q "HelpLinkUri" CONTRIBUTING.md && grep -q "docs/diagnostics.md" CONTRIBUTING.md</automated>
  </verify>
  <done>
    CONTRIBUTING.md updated with:
    - Platform Constraints section with netstandard2.0 explanation
    - Link to docs/platform-constraints.md
    - Updated Diagnostic Guidelines section referencing docs/diagnostics.md
    - HelpLinkUri format documentation
  </done>
</task>

</tasks>

<verification>
- docs/diagnostics.md has category navigation at top
- All diagnostics have severity badges ([!Error], [!Warning], [!Info])
- Related diagnostics are cross-referenced
- IOC090-IOC094 (typeof() diagnostics) are documented
- Testing Diagnostics section (TDIAG-01 through TDIAG-05) exists
- All HelpLinkUri anchors (like #ioc001) resolve to actual sections
- CONTRIBUTING.md has Platform Constraints section with link to docs/platform-constraints.md
- Diagnostic Guidelines section references docs/diagnostics.md and HelpLinkUri format
</verification>

<success_criteria>
1. docs/diagnostics.md is organized by category with navigation
2. All 99 diagnostics (IOC001-IOC094, TDIAG-01 through TDIAG-05) are documented
3. Severity badges make visual scanning easier
4. Cross-references help users understand related diagnostics
5. CONTRIBUTING.md helps contributors understand platform constraints
6. All HelpLinkUri values in codebase resolve to docs/diagnostics.md anchors
</success_criteria>

<output>
After completion, create `.planning/phases/04-documentation/04-04-SUMMARY.md` with:
- Category list added to docs/diagnostics.md
- Count of diagnostics with severity badges
- Confirmation of typeof() diagnostics (IOC090-IOC094)
- Confirmation of HelpLinkUri anchor verification
- CONTRIBUTING.md updates summary
</output>
