---
phase: 04-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - CHANGELOG.md
autonomous: true
requirements:
  - DOC-01
  - DOC-02
  - DOC-08
must_haves:
  truths:
    - README.md is reduced to 250-300 lines (currently 537 lines)
    - README.md has clear sections: what, why, install, quickstart, key benefits, links to deeper docs
    - CHANGELOG.md exists at repository root with version history
    - README "What's New in v1.5.0" section exists with max 4 bullets
    - README "Platform Support" section answers "Will this work in my project?"
    - Links from README point to /docs/ files that exist
  artifacts:
    - path: "README.md"
      provides: "Lean 250-300 line landing page"
      max_lines: 300
      min_sections:
        - Highlights
        - Installation
        - What's New in v1.5.0
        - Getting Started
        - Platform Support
        - Testing with IoCTools
        - CLI
        - Links
    - path: "CHANGELOG.md"
      provides: "Version history following Keep a Changelog format"
      contains: "## [1.5.0]"
    - path: "docs/"
      provides: "Documentation directory"
      exists: true
  key_links:
    - from: "README.md"
      to: "docs/getting-started.md"
      via: "relative markdown link"
      pattern: "\\[.*\\]\\(docs/getting-started\\.md\\)"
    - from: "README.md"
      to: "docs/testing.md"
      via: "relative markdown link"
      pattern: "\\[.*\\]\\(docs/testing\\.md\\)"
    - from: "README.md"
      to: "docs/attributes.md"
      via: "relative markdown link"
      pattern: "\\[.*\\]\\(docs/attributes\\.md\\)"
    - from: "README.md"
      to: "CHANGELOG.md"
      via: "relative markdown link"
      pattern: "\\[.*\\]\\(CHANGELOG\\.md\\)"
---

<objective>
Restructure README.md to a lean 250-300 line landing page and create CHANGELOG.md for version history.

Purpose: Transform the current 537-line comprehensive README into an entry point that balances new user onboarding with links to deeper documentation, following CONTEXT.md decisions D-01 through D-05 and D-20 through D-25.

Output: Lean README.md (250-300 lines) + CHANGELOG.md with v1.5.0 release notes
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@README.md
@.planning/phases/04-documentation/04-CONTEXT.md
@.planning/phases/04-documentation/04-RESEARCH.md
@.planning/phases/01-code-quality-diagnostic-ux/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CHANGELOG.md with v1.5.0 release notes</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - .planning/phases/04-documentation/04-CONTEXT.md (D-21: CHANGELOG.md format)
    - README.md (existing content to extract version history from)
    - .planning/ROADMAP.md (v1.5.0 feature summary)
  </read_first>
  <action>
Create CHANGELOG.md at repository root following "Keep a Changelog" format with these sections:

1. Header: "# Changelog\n\nAll notable changes to this project will be documented in this file.\n\nThe format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).\n"

2. v1.5.0 section (released 2026-03-21):
   ## [1.5.0] - 2026-03-21

   ### Added
   - typeof() registration diagnostics (IOC090-094) for build-time validation of manual DI registrations
   - IoCTools.Testing package with [Cover&lt;T&gt;] attribute for auto-generated test fixtures (Mock&lt;T&gt; fields, CreateSut(), setup helpers)
   - Test fixture analyzer diagnostics (TDIAG-01 through TDIAG-05) suggesting fixture usage
   - CLI improvements: --json output mode, --verbose debugging, color-coded severity output
   - CLI wildcard filtering and fuzzy suggestions for type names
   - CLI `config-audit` command for detecting missing configuration keys
   - CLI `suppress` command for generating .editorconfig diagnostic suppression recipes

   ### Changed
   - Enhanced IOC012/IOC013/IOC087 messages with IServiceProvider/CreateScope() suggestions
   - IOC015 now shows full inheritance path (A -> B -> C format) for lifetime mismatches
   - All 94+ diagnostics now have HelpLinkUri pointing to docs/diagnostics.md#iocXXX
   - Diagnostic messages reference individual lifetime attributes ([Scoped], [Singleton], [Transient])

   ### Fixed
   - CS8603 null reference warnings in sample code
   - Exception handling in ConstructorGenerator, InterfaceDiscovery, and ServiceRegistrationGenerator

   ### Diagnostic
   - 5 new typeof() diagnostics (IOC090-IOC094)
   - 5 new test fixture diagnostics (TDIAG-01 through TDIAG-05)

3. Empty "## [Unreleased]" section at top for future changes

Do NOT include links section at bottom (that comes in later plan).
  </action>
  <verify>
    <automated>grep -q "## \[1\.5\.0\]" CHANGELOG.md && grep -q "### Added" CHANGELOG.md && grep -q "IoCTools\.Testing" CHANGELOG.md</automated>
  </verify>
  <done>
    CHANGELOG.md exists at repository root with:
    - Keep a Changelog format header
    - ## [1.5.0] section with Added/Changed/Fixed/Diagnostic subsections
    - typeof() diagnostics, IoCTools.Testing, CLI improvements documented
    - Empty ## [Unreleased] section at top
  </done>
</task>

<task type="auto">
  <name>Task 2: Restructure README.md to lean 250-300 line landing page</name>
  <files>README.md</files>
  <read_first>
    - README.md (full current content to understand what to keep/extract/link)
    - .planning/phases/04-documentation/04-CONTEXT.md (D-01 through D-05, D-20 through D-25 for structure)
    - .planning/phases/04-documentation/04-RESEARCH.md (for recommended sections)
  </read_first>
  <action>
Restructure README.md to 250-300 lines following this exact structure:

1. **Header (unchanged)**: Keep existing NuGet badges and tagline

2. **Highlights section (condensed)**: Keep 6 bullet points max, trim verbose descriptions

3. **Installation section (unchanged)**: Keep existing install commands

4. **NEW: What's New in v1.5.0** (after Installation, before Getting Started):
   ```markdown
   ## What's New in v1.5.0

   - [Test fixture generation](docs/testing.md) — Auto-generate Mock fields, CreateSut(), and setup helpers with [Cover\<T\>](docs/testing.md)
   - [typeof() diagnostics](docs/diagnostics.md#ioc090) — Build-time validation for manual DI registrations (IOC090-IOC094)
   - [CLI enhancements](docs/cli-reference.md) — JSON output, verbose mode, color-coded diagnostics, and config-audit
   - [Improved diagnostics](docs/diagnostics.md) — Enhanced messages with CreateScope() suggestions and inheritance paths
   ```

5. **Getting Started section (condensed)**: Keep 3-step format but trim explanatory text, add link to docs/getting-started.md

6. **NEW: Platform Support section** (2-3 sentences per D-27):
   ```markdown
   ## Platform Support

   IoCTools works with .NET Framework 4.6.1+, .NET Core 2.0+, and .NET 5+. The generator targets netstandard2.0 internally, but your service code can use any C# features your framework supports. See [platform constraints](docs/platform-constraints.md) for details.
   ```

7. **Testing with IoCTools section (new 20-line teaser per D-13)**:
   ```markdown
   ## Testing with IoCTools

   The [IoCTools.Testing](docs/testing.md) package auto-generates test fixtures, eliminating mock declaration boilerplate.

   ```csharp
   using IoCTools.Testing.Annotations;

   [Cover\<UserService\>]
   public partial class UserServiceTests
   {
       [Fact]
       public void Test() {
           var sut = CreateSut(); // Auto-generated
       }
   }
   ```

   [Full testing guide](docs/testing.md)
   ```

8. **CLI section (condensed to table + brief description, link to docs/cli-reference.md)**: Keep command table but trim explanation text

9. **Before & After section (KEEP - demonstrates value per D-09)**: Keep this as-is, it's the strongest value proposition

10. **Attribute Reference section (convert to link-only)**: Replace full table with:
    ```markdown
    ## Attribute Reference

    Complete attribute reference: [docs/attributes.md](docs/attributes.md)

    Key attributes: `[Scoped]`, `[Singleton]`, `[Transient]`, `[DependsOn\<T\>]`, `[RegisterAs\<T\>]`, `[ConditionalService]`, `[InjectConfiguration]`
    ```

11. **Diagnostics Reference section (Error-only table + link)**:
    ```markdown
    ## Diagnostics Reference

    IoCTools provides 94+ diagnostics (IOC001-IOC094, TDIAG-01 through TDIAG-05). See [complete reference](docs/diagnostics.md).

    ### Error-Severity Diagnostics

    | Rule | Summary |
    |------|---------|
    | IOC001 | Service depends on unimplemented interface |
    | IOC002 | Implementation missing lifetime attribute |
    | IOC003 | Circular dependency detected |
    | IOC004 | [RegisterAsAll] requires lifetime |
    | IOC011 | Background service must be partial |
    | IOC012 | Singleton depends on Scoped |
    | IOC014 | Background service with non-Singleton lifetime |
    | IOC015 | Lifetime mismatch in inheritance chain |
    | IOC016 | Invalid configuration key |
    | IOC018 | [InjectConfiguration] requires partial class |
    | IOC021 | [ConditionalService] requires lifetime |
    | IOC028 | [RegisterAs] without service indicators |
    | IOC029 | [RegisterAs] specifies unimplemented interface |
    | IOC031 | [RegisterAs] specifies non-interface type |
    | IOC041 | Manual constructor conflicts with IoCTools |
    | IOC049 | Dependency set with non-metadata members |
    | IOC050 | Dependency set cycle detected |
    | IOC051 | Dependency set name collision |
    | IOC077 | Manual field shadows generated dependency |
    | IOC080 | Code-generating attributes require partial |
    | IOC081 | Manual registration duplicates IoCTools |
    | IOC082 | Manual registration lifetime differs |
    | IOC087 | Transient depends on Scoped |
    | IOC088 | Configuration circular reference |
    | IOC092 | typeof() registration lifetime mismatch |
    | TDIAG-04 | [Cover\<T\>] service has no generated constructor |
    | TDIAG-05 | Test class with [Cover\<T\>] must be partial |

    [View all diagnostics including warnings and info](docs/diagnostics.md)
    ```

12. **Configuration section (condensed to table + link)**: Keep MSBuild property table but add "See [configuration reference](docs/configuration.md)" after it

13. **Samples & License section (unchanged)**: Keep as-is

14. **Remove entirely**: "Future Ideas" section (this is internal planning, not user-facing)
  </action>
  <verify>
    <automated>wc -l README.md | awk '{if ($1 <= 300) print "PASS"; else print "FAIL: " $1 " lines exceeds 300"}' && grep -q "## What's New in v1.5.0" README.md && grep -q "## Platform Support" README.md && grep -q "## Testing with IoCTools" README.md && grep -q "docs/getting-started.md" README.md && grep -q "docs/testing.md" README.md && grep -q "docs/attributes.md" README.md && grep -q "docs/cli-reference.md" README.md && grep -q "docs/platform-constraints.md" README.md</automated>
  </verify>
  <done>
    README.md restructured to 250-300 lines with:
    - "What's New in v1.5.0" section with 4 bullets linking to docs
    - "Platform Support" section answering compatibility question
    - "Testing with IoCTools" 20-line teaser with code example and link
    - Attribute Reference replaced with link to docs/attributes.md
    - Error-only diagnostic table (~30 rows) with link to full reference
    - CLI section condensed with link to docs/cli-reference.md
    - Configuration section retained with link to docs/configuration.md
    - "Future Ideas" section removed
  </done>
</task>

</tasks>

<verification>
- README.md line count is 250-300 lines (verify with `wc -l README.md`)
- All new /docs/ links in README.md resolve to files created in later plans (getting-started.md, testing.md, attributes.md, cli-reference.md, platform-constraints.md, configuration.md)
- CHANGELOG.md exists with v1.5.0 section covering all new features
- Links use relative paths (docs/filename.md format)
- NuGet badges and tagline remain at top of README
- Before & After section preserved (key value proposition)
</verification>

<success_criteria>
1. A new user can read README.md in under 2 minutes and understand what IoCTools does and how to install
2. README.md provides clear navigation paths to detailed documentation via /docs/ links
3. CHANGELOG.md provides industry-standard version history
4. README.md fits in 250-300 lines (measured, verified)
5. "Platform Support" section prevents "Will this work in my project?" questions
</success_criteria>

<output>
After completion, create `.planning/phases/04-documentation/04-01-SUMMARY.md` with:
- Actual README.md line count after restructuring
- List of /docs/ links added (for verification in later plans)
- CHANGELOG.md sections created
</output>
